⚙️ متطلبات التشغيل (Setup Requirements)

1. تثبيت Node.js (إصدار LTS)
يُنصح بتثبيت الإصدار طويل الدعم (LTS) من Node.js لضمان استقرار المشروع وتشغيله بدون مشاكل.


2. إعداد قاعدة البيانات (MongoDB Atlas)

يُفضل إنشاء حساب مجاني على MongoDB Atlas.

بعد إنشاء الـ cluster، انسخ رابط الاتصال (Connection String).

أضفه إلى ملف البيئة .env بهذا الشكل:

MONGO_URI=رابط_الاتصال_من_Atlas



3. إعداد GitHub (اختياري في حال رفع المشروع أو استخدام الـ API)

أنشئ مستودعًا (repository) جديدًا باسم مثل:

Tager-Image

من إعدادات الحساب (Settings) → Developer Settings → Personal Access Tokens.

اختر Tokens (classic) ثم اضغط Generate new token.

اختر النوع الذي يحتوي على كلمة Classic.

أضف وصفًا (Note) للـ token وحدد الصلاحيات المطلوبة — على الأقل:

repo

بعد الإنشاء، انسخ الـ token وأضفه إلى ملف .env مع باقي البيانات مثلاً:

GITHUB_TOKEN=توكن_الحساب
REPO_NAME=Tager-Image
REPO_OWNER=اسم_المستخدم





---

💡 ملاحظات إضافية:

تأكد من وجود ملف .env في نفس مجلد المشروع (root folder).

لا ترفع هذا الملف إلى GitHub (أضفه إلى .gitignore).

استخدم npm install لتثبيت المكتبات ثم npm run dev لتشغيل السيرفر أثناء التطوير.